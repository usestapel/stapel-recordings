{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "recording.stage",
  "description": "Internal driver step: run stage N of the resolved pipeline.",
  "type": "object",
  "additionalProperties": false,
  "required": ["recording_id", "stage_index"],
  "properties": {
    "recording_id": {"type": "string"},
    "stage_index": {"type": "integer", "minimum": 0}
  }
}
